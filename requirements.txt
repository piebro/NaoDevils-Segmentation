imgaug==0.2.9
matplotlib==3.1.3
numpy==1.17.5
Keras==2.2.5
tensorflow>=2.1.0
opencv-python==4.2.0.32
pypng==0.0.20
protobuf==3.10.0
kaggle==1.5.6
#pycocotools==2.0.0 should be installed too, but the setup py needs numpy already installed for some reason, install it with pip install pycocotools==2.0.0 manually later